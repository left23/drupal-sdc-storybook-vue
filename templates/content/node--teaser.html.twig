{# {{ include('emc:title', {
    text: label,
    level: 3
  }, with_context = false)
}}

{{ include('emc:title', {
    text: label,
    level: 4
  })
}} #}

{# {{ content }} #}



{{ include('emc:card', {
    text: content,
    level: 4,
    title: label,
  })
}}
